# Tiny LiteRT baseline

This case uses the immutable TensorFlow Lite Micro `hello_world_float.tflite`
fixture to establish a small real-model baseline. The WASM expectation is
passing. The WebGPU environment is selected only when the browser reports
WebGPU support.
