# Tiny LiteRT baseline

This case uses the immutable LiteRT.js core `add_10x10.tflite` fixture to
establish a small real-model baseline. The WASM expectation is passing. The
WebGPU environment is selected only when the browser reports WebGPU support.
