# Qwen XNNPACK prefill

This case isolates the browserMemory Qwen prefill failure recorded in the
Playground verification notes. The expected stage is prefill. The model
descriptor remains absent until a real reproduction records immutable asset
metadata; the case must not invent a URL, size, or hash.
